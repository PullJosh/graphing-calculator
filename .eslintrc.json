{
  "extends": "next/core-web-vitals",
  "rules": {
    "react/no-unescaped-entities": "off",

    // The following rule causes problems with react-three-fiber, so we disable it
    "react/no-unknown-property": "off"
  }
}
